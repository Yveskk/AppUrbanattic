import 'package:flutter/material.dart';

class Archiver extends StatelessWidget {
  const Archiver({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}